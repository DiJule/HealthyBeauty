# Healthy Beauty - E-Commerce Platform

Повнофункціональна платформа електронної комерції для продажу товарів красоти та здоров'я з інтегрованою системою аналізу продуктів на основі штучного інтелекту.

**Автор:** Душенко Юля, студентка групи ІП-22-4

---

## 📋 Описання проекту

**Healthy Beauty** — це сучасна веб-платформа, що дозволяє:

- 🛍️ Переглядати та фільтрувати широкий асортимент товарів красоти та здоров'я
- 💬 Залишати та читати відгуки про продукти
- 🤖 Отримувати АІ-аналіз характеристик, переваг та способу використання продуктів
- 🛒 Управляти кошиком для покупок в реальному часі
- 💳 Проводити платежі через LiqPay
- 📦 Отримувати інформацію про статус замовлень
- 💬 Спілкуватися з чат-ботом для отримання рекомендацій
- 👨‍💼 Адміністраторам керувати каталогом, замовленнями та відгуками

---

## 🎯 Основні можливості

### Для покупців
- **Каталог продуктів** — фільтрація за категоріями, пошук
- **Детальна інформація** — опис, характеристики, переваги та інструкції використання
- **Система відгуків** — читання та написання рецензій інших користувачів
- **АІ аналіз продуктів** — локальний аналіз та генерація описів
- **Управління профілем** — дані користувача, історія замовлень
- **Кошик для покупок** — редагування кількості, видалення товарів
- **Безпечне оплачування** — інтеграція LiqPay
- **Обробка замовлень** — відслідковування статусу
- **Чат-асистент** — отримання допомоги та рекомендацій

### Для адміністраторів
- **Управління продуктами** — додавання, редагування, видалення товарів
- **Керування категоріями** — організація каталогу
- **Модерація відгуків** — перевірка та затвердження рецензій
- **Управління замовленнями** — відслідковування та оновлення статусу
- **Аналіз даних** — перегляд активності користувачів

---

## 🛠️ Технічний стек

### Backend
- **Ruby on Rails** 7.2.2.2 — веб-фреймворк
- **SQLite3** — база даних для розробки
- **PostgreSQL** — база даних для production
- **Devise** — управління аутентифікацією користувачів

### Frontend
- **HTML5, CSS3, JavaScript (ES6+)**
- **Turbo** — навігація без перезавантаження сторінки
- **Stimulus** — інтерактивність
- **Sprockets Rails** — asset pipeline

### DevOps & Deployment
- **Docker** — контейнеризація приложення
- **Render** — хостинг та CI/CD
- **Redis** — кешування та сесії

### Інші модулі
- **Faraday** — HTTP клієнт
- **Dotenv** — управління environment variables
- **BCrypt** — шифрування паролів

---

## 📦 Вимоги до системи

### Розробка
- **Ruby** 3.3.5+
- **Rails** 7.2.2+
- **SQLite3** 3.8+
- **Bundler** 2.0+
- **Node.js** 18+ (для asset compilation)

### Production
- **Docker** 20.10+
- **PostgreSQL** 13+
- **Redis** 6.0+

---

## 🚀 Встановлення та запуск

### 1. Клонування репозиторію
```bash
git clone https://github.com/your-repo/healthybeauty.git
cd healthybeauty
```

### 2. Встановлення залежностей
```bash
bundle install
yarn install  # або npm install
```

### 3. Конфігурація базі даних
```bash
rails db:create
rails db:migrate
rails db:seed  # завантаження базових даних
```

### 4. Налаштування environment variables
Створіть файл `.env.development`:
```env
RAILS_ENV=development
LIQPAY_PUBLIC_KEY=your_liqpay_public_key
LIQPAY_PRIVATE_KEY=your_liqpay_private_key
GEMINI_API_KEY=your_gemini_api_key  # опціонально
RAILS_MASTER_KEY=your_master_key
```

### 5. Запуск сервера розробки
```bash
rails server
```

Програма буде доступна за адресою: `http://localhost:3000`

---

## 🐳 Docker & Production Deployment

### Локальний Docker build
```bash
docker build -t healthybeauty .
docker run -p 3000:3000 \
  -e RAILS_ENV=production \
  -e DATABASE_URL=postgresql://user:pass@host:5432/dbname \
  -e RAILS_MASTER_KEY=your_key \
  healthybeauty
```

### Deployment на Render.com
Див. детальні інструкції у `DEPLOY.md`:
```bash
git add .
git commit -m "Deployment configuration"
git push origin main
```

Потім у Render Dashboard:
1. Створити новий Web Service
2. Обрати GitHub репозиторій
3. Налаштувати PostgreSQL database
4. Встановити environment variables
5. Запустити deployment

---

## 📁 Структура проекту

```
.
├── app/
│   ├── assets/              # Статичні ресурси (CSS, зображення)
│   ├── channels/            # WebSocket каналів (Action Cable)
│   ├── controllers/         # HTTP контролери
│   │   ├── admin_controller.rb
│   │   ├── products_controller.rb
│   │   ├── carts_controller.rb
│   │   ├── orders_controller.rb
│   │   └── ...
│   ├── models/              # Моделі даних
│   │   ├── user.rb
│   │   ├── product.rb
│   │   ├── order.rb
│   │   ├── review.rb
│   │   └── ...
│   ├── views/               # HTML шаблони
│   │   ├── products/
│   │   ├── orders/
│   │   ├── carts/
│   │   ├── admin/
│   │   └── ...
│   ├── helpers/             # Допоміжні методи для views
│   └── mailers/             # Email шаблони
│
├── config/
│   ├── routes.rb            # Маршрути додатку
│   ├── database.yml         # Конфіг БД (SQLite3, PostgreSQL)
│   ├── credentials.yml.enc  # Зашифровані облікові дані
│   ├── environments/        # Конфіги для dev, test, production
│   └── initializers/        # Ініціалізатори gems
│
├── db/
│   ├── migrate/             # Міграції БД
│   ├── schema.rb            # Поточна схема БД
│   └── seeds.rb             # Базові дані
│
├── lib/
│   ├── assets/              # Додаткові asset файли
│   └── tasks/               # Custom Rake завдання
│
├── public/                  # Статичні файли (HTML помилок, robots.txt)
├── storage/                 # Локальне зберігання файлів
├── test/                    # Тести (unit, integration, system)
├── vendor/                  # Залежності, встановлені вручну
│
├── bin/
│   ├── rails                # Rails CLI
│   ├── rake                 # Rake CLI
│   └── docker-entrypoint    # Entry point для Docker контейнера
│
├── Dockerfile               # Docker конфіг для production
├── Gemfile                  # Ruby gems залежності
├── Gemfile.lock             # Locked версії gems
├── .env.development         # Development environment variables
├── .gitignore               # Git ignore файли
├── render.yaml              # Render deployment конфіг
├── DEPLOY.md                # Детальна інструкція deployment
├── DEPLOYMENT_CHECKLIST.md  # Чек-лист перед deployment
└── README.md                # Цей файл
```

---

## 🗄️ Моделі даних

### User (Користувач)
```
- email (string, unique)
- password (encrypted)
- name (string)
- phone (string)
- address (text)
- role (enum: user, admin)
- created_at, updated_at
```

### Product (Продукт)
```
- name (string)
- description (text)
- price (decimal)
- category_id (foreign key)
- characteristics (text) — АІ-генеровано
- benefits (text) — АІ-генеровано
- usage (text) — АІ-генеровано
- image_url (string)
- stock (integer)
- created_at, updated_at
```

### Order (Замовлення)
```
- user_id (foreign key)
- total_amount (decimal)
- status (enum: pending, confirmed, shipped, delivered)
- delivery_address (text)
- delivery_option_id (foreign key)
- payment_id (foreign key)
- created_at, updated_at
```

### Review (Відгук)
```
- product_id (foreign key)
- user_id (foreign key)
- rating (integer, 1-5)
- comment (text)
- created_at, updated_at
```

### Cart & CartItem (Кошик)
```
Cart:
  - user_id (foreign key)
  - created_at, updated_at

CartItem:
  - cart_id (foreign key)
  - product_id (foreign key)
  - quantity (integer)
  - created_at, updated_at
```

---

## 🔐 Безпека

- **Аутентифікація** — Devise gem з хешуванням паролів BCrypt
- **CSRF захист** — Rails вбудований захист від CSRF атак
- **SQL Injection захист** — Rails параметризовані запити
- **Шифрування даних** — Rails credentials для конфіденційної інформації
- **HTTPS** — обов'язковий для production (Render SSL)
- **Environment variables** — конфіденційні дані не зберігаються у коді

---

## 📝 API Endpoints

### Продукти
- `GET /products` — список всіх продуктів
- `GET /products/:id` — деталі продукту
- `POST /products/:id/analyze_with_ai` — АІ аналіз продукту
- `GET /categories` — список категорій

### Користувачі
- `POST /users/sign_up` — реєстрація
- `POST /users/sign_in` — вхід
- `DELETE /users/sign_out` — вихід
- `GET /users/profile` — профіль користувача

### Замовлення
- `GET /orders` — мої замовлення
- `POST /orders` — створення замовлення
- `GET /orders/:id` — деталі замовлення

### Кошик
- `GET /carts` — вміст кошика
- `POST /cart_items` — додавання товару
- `DELETE /cart_items/:id` — видалення товару

### Адміністрування
- `GET /admin` — адмін панель (лише для адмінів)
- `GET /admin/products` — управління продуктами
- `GET /admin/orders` — управління замовленнями
- `GET /admin/reviews` — модерація відгуків

---

## 🧪 Тестування

### Запуск тестів
```bash
rails test                          # всі тести
rails test:system                   # системні тести
rails test:models                   # тести моделей
rails test:controllers              # тести контролерів
```

---

## 🚨 Troubleshooting

### "Gem не встановлено"
```bash
bundle install
```

### "БД не знайдена"
```bash
rails db:create
rails db:migrate
```

### "Assets не компілюються"
```bash
rails assets:precompile
```

### "RAILS_MASTER_KEY не знайдено"
Переконайтесь, що файл `config/master.key` існує або встановіть environment variable `RAILS_MASTER_KEY`

### Docker build помилки
Див. розділ "Troubleshooting" у `DEPLOY.md`

---

## 📚 Додаткова документація

- **[DEPLOY.md](./DEPLOY.md)** — Детальна інструкція deployment на Render.com
- **[DEPLOYMENT_CHECKLIST.md](./DEPLOYMENT_CHECKLIST.md)** — Чек-лист перед deploy
- [Rails Guides](https://guides.rubyonrails.org/) — Офіційна документація Rails
- [Devise Documentation](https://github.com/heartcombo/devise) — Аутентифікація
- [Render Docs](https://render.com/docs) — Документація хостингу

---

## 🤝 Внесення змін

1. Fork репозиторій
2. Створити гілку для фічі (`git checkout -b feature/amazing-feature`)
3. Скомітити зміни (`git commit -m 'Add amazing feature'`)
4. Запушити гілку (`git push origin feature/amazing-feature`)
5. Відкрити Pull Request

---

## 👩‍💻 Автор

**Душенко Юля**  
Студентка групи ІП-22-4  
[GitHub](https://github.com/DiJule)

---

## 🙏 Подяки

- Rails Community за чудовий фреймворк
- Devise команді за аутентифікацію
- LiqPay за інтеграцію платежів
- Render за простоту deployment

---

**Версія:** 1.0.0  
**Останнє оновлення:** Грудень 2025  
**Статус:** ✅ Production Ready
