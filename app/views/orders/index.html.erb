<div class="orders-page" style="background:#f8f8f8; max-width:900px; margin:2em auto; padding:2em 1em; border-radius:18px; box-shadow:0 2px 16px #e0e0e0;">
  <h1 class="section-title"><i class="fa fa-list" style="color:#009a09;"></i> Мої замовлення</h1>
  <% if @orders.any? %>
    <table class="pure-table pure-table-bordered" style="width:100%;">
      <thead>
        <tr>
          <th>№</th>
          <th>Статус</th>
          <th>Сума</th>
          <th>Доставка</th>
          <th>Дата</th>
          <th>Деталі</th>
        </tr>
      </thead>
      <tbody>
        <% @orders.each do |order| %>
          <tr>
            <td><%= order.id %></td>
            <td><%= order.status %></td>
            <td><%= order.total %> грн</td>
            <td><%= order.delivery_method %></td>
            <td><%= order.created_at.strftime('%d.%m.%Y %H:%M') %></td>
            <td><%= link_to 'Переглянути', order_path(order), class: 'pure-button' %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% else %>
    <p>У вас ще немає замовлень.</p>
  <% end %>
  <div style="margin-top:1em;">
    <%= link_to 'На головну', root_path, class: 'pure-button' %>
  </div>
</div>
